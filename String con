#include <stdio.h>
#include <string.h>

int main() {
    char str1[50] = "Hello ";
    char str2[20] = "World";

    strcat(str1, str2); 

    printf("Concatenated String: %s", str1);

    return 0;
}
